/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,


  // this is used for image that are copy ulr from websites.
  images: {
    domains: ["images.pexels.com"],


  },
};

export default nextConfig;
